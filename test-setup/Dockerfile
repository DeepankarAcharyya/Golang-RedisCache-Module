# Use the official Redis image from Docker Hub
FROM redis:latest

# Set the password as an environment variable (you can modify the password here)
ENV REDIS_PASSWORD=mystrongpassword

# Expose the default Redis port
EXPOSE 6379

# Start Redis server
CMD ["redis-server"]
